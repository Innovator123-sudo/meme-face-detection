name: ci
on:
  push:
    branches: [main]
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - name: Restore RMN weights
        uses: actions/cache@v4
        with:
          path: public/rmn/resmasking_int8.onnx
          key: rmn-int8-v1
      - run: npm ci
      - run: npm run test:accuracy
      - run: node scripts/test-online-client.mjs
      - run: node scripts/test-beast-fusion.mjs
      - run: npm run lint
      - run: npm run build
